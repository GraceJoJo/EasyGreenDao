package com.jojo.sample.easygreendao.db.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.jojo.sample.easygreendao.db.bean.AuthorBean;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "AUTHOR_BEAN".
*/
public class AuthorBeanDao extends AbstractDao<AuthorBean, Long> {

    public static final String TABLENAME = "AUTHOR_BEAN";

    /**
     * Properties of entity AuthorBean.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Author_name = new Property(1, String.class, "author_name", false, "AUTHOR_NAME");
        public final static Property Identity = new Property(2, String.class, "identity", false, "IDENTITY");
    }


    public AuthorBeanDao(DaoConfig config) {
        super(config);
    }
    
    public AuthorBeanDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"AUTHOR_BEAN\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"AUTHOR_NAME\" TEXT," + // 1: author_name
                "\"IDENTITY\" TEXT);"); // 2: identity
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"AUTHOR_BEAN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, AuthorBean entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String author_name = entity.getAuthor_name();
        if (author_name != null) {
            stmt.bindString(2, author_name);
        }
 
        String identity = entity.getIdentity();
        if (identity != null) {
            stmt.bindString(3, identity);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, AuthorBean entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String author_name = entity.getAuthor_name();
        if (author_name != null) {
            stmt.bindString(2, author_name);
        }
 
        String identity = entity.getIdentity();
        if (identity != null) {
            stmt.bindString(3, identity);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public AuthorBean readEntity(Cursor cursor, int offset) {
        AuthorBean entity = new AuthorBean( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // author_name
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2) // identity
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, AuthorBean entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setAuthor_name(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setIdentity(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(AuthorBean entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(AuthorBean entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(AuthorBean entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
